const slider_container = document.querySelector('.reviews__row');
const left_button = document.getElementById('left-button');
const right_button = document.getElementById('right-button');

function thenItemDisapper(e) {

}

Array.from(slider_container.children).forEach(e => console.log(e));
